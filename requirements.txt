# FastAPI 서버
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
jinja2==3.1.3

# AI 모델 연동
google-generativeai==0.3.2
google-genai
openai==1.12.0

# 이미지 처리
Pillow==10.2.0
numpy

# 텍스트 렌더링 (한글 말풍선, 캐러셀, 카드뉴스)
playwright

# HTTP 요청
httpx==0.26.0
aiohttp==3.9.3

# 환경 변수
python-dotenv==1.0.1

# 데이터 검증
pydantic==2.6.0
pydantic-settings==2.1.0

# 배포용
pyinstaller==6.3.0

# Cloudinary (이미지 호스팅 — httpx로 직접 호출, SDK 불필요)
